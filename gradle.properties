org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Android Studio ships a JDK 25 that current AGP does not support. Pin both the CLI
# and the IDE to the JDK 21 in ~/.jdks so they share one Gradle daemon and one cache.
org.gradle.java.home=/Users/Rachel/.jdks/jdk-21.0.12+8/Contents/Home
